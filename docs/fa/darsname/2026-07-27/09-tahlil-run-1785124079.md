# درس ۹ — تحلیل run exploratory `1785124079`

> **تحلیل تاریخی یک run schema v5/model v1 است.** این artifact شاهد v6 یا دلیل
> برتری/سودآوری فعلی نیست؛ مراجع v6 و وضعیت شواهد را برای تفسیر فعلی بخوانید.

## مشخصات نسخه

| فیلد | مقدار |
|------|--------|
| تاریخ درس | 2026-07-27 |
| snapshot | [SYSTEM_SNAPSHOT-2026-07-27.md](../SYSTEM_SNAPSHOT-2026-07-27.md) |
| run ID | `btcusdt-quantum-1785124079` |
| schema | v5 |
| horizon | 60s (exploratory) |
| model | `born_constructive_v1` |
| لاگ | `live_exploratory.log` (از خط ~4696) |
| خروجی | `core/_live_results/btcusdt-quantum-1785124079.{json,sqlite3}` |
| artifact | [exploratory-run-1785124079-2026-07-27.md](../../../../artifacts/exploratory-run-1785124079-2026-07-27.md) |
| پیش‌نیاز | [L07](./07-tahlil-va-adam-claim.md), [L06](./06-dow-run-parallel.md) |

### تغییرات نسبت به L07

- اولین درس **case study** روی corpus واقعی n=280 (نه ref کوچک n=50)
- ریشه‌یابی ۶ «ایراد» مشاهده‌شده در log زنده
- نتیجه analyzer رسمی + مقایسه با ref `1785119130`

---

## سوالات این درس

1. آیا نتایج exploratory واقعاً بهتر شده؟
2. خطوط `SKIP forecast — pending unresolved` یعنی چه؟
3. چرا `saturation_gate` این‌قدر زیاد است؟
4. چرا بعضی resolveها `c_err` خیلی بالا دارند؟
5. null baseline (0.5) هنوز بهتر است — باگ است؟
6. چه claimهایی مجاز و ممنوع است؟

---

## ۱. خلاصه run

| مورد | مقدار |
|------|--------|
| شروع | 2026-07-27 07:17:59 |
| پایان | 2026-07-27 13:59:51 (`user_stop` + قطع شبکه) |
| observation | 2885 |
| prediction | 319 (1 pending، 280 resolved eligible) |
| بازه داده | 07:19:47 → 12:58:53 |
| config hash | `b6416c48177a29892fe261273f175c6d6d9da1e665127ef273a4cb1bae06df27` |

دستور تحلیل:

```bash
python3 core/analyze_live_results.py --schema-version 5 \
  core/_live_results/btcusdt-quantum-1785124079.json
```

---

## ۲. نتایج aggregate (n=280 eligible)

| معیار | ref قدیم `1785119130` (n=50) | run جدید (n=280) |
|--------|------------------------------|------------------|
| Classical MAE | 0.304 | **0.3183** |
| Quantum MAE | 0.321 | **0.3078** |
| Null MAE(0.5) | 0.252 | **0.2824** |
| Quantum vs Classical | quantum بدتر | **quantum بهتر (+3.30%)** |
| Quantum wins | 48% | **50.0%** |
| forward_window | 100% | **100%** (280/280) |
| Born active | 22% | **34.6%** (97/280) |
| saturation_gate | 78% | **65.4%** (183/280) |

### جمع‌بندی «بهتر شده؟»

**بله، با محدودیت:**

- quantum برای اولین بار در corpus بزرگ‌تر از classical جلو افتاده (0.3078 < 0.3183).
- segment `born_active_only` (n=97): improvement **+7.18%** — بهتر از aggregate.
- segment `saturation_gate` (n=183): improvement **+1.11%** — تقریباً برابر classical.

**اما:**

1. **Null baseline (0.2824) هنوز از هر دو بهتر است** — روی horizon 60s طبیعی است (نویز microstructure).
2. **wins=50%** = برابر شانس؛ edge قابل claim نیست.
3. **Exploratory 60s ≠ evidence برای production 3600s.**

---

## ۳. روند MAE در طول run (از log)

```text
n=50:   c=0.3197  q=0.3014  null=0.2917  wins=54%
n=100:  c=0.3381  q=0.3126  null=0.2837  wins=52%
n=200:  c=0.3354  q=0.3151  null=0.2877  wins=52%
n=250:  c=0.3230  q=0.3114  null=0.2814  wins=51%
n=280:  c=0.3183  q=0.3078  null=0.2824  wins=50%  ← FINAL
```

MAE با n بیشتر **متمرکزتر** شده. جهش n=100–200 احتمالاً نمونه کوچک + چند resolve پرخطا بوده، نه regression مدل.

---

## ۴. نمونه log (resolveهای 277–280)

بخشی که در ترمینال دیده شد (خطوط 8169–8203 در `live_exploratory.log`):

```text
[2851] 12:55:43 RESOLVED act=0.600 c_err=0.223 q_err=0.064
[2860] 12:56:46 RESOLVED act=0.118 c_err=0.554 q_err=0.646
[2868] 12:57:46 RESOLVED act=0.667 c_err=0.379 q_err=0.213  fallback=none
[2877] 12:58:53 RESOLVED act=0.444 c_err=0.114 q_err=0.123
--- resolved 280: MAE c=0.3183 q=0.3078 null(0.5)=0.2824 wins=140/280 (50%)
```

- **2851:** quantum خطای کم (0.064) — یکی از resolveهای خوب.
- **2860:** `act=0.118` (sell شدید) ولی `c=0.288` — بازار flip؛ هر دو مدل miss.
- **2868:** تنها forecast با `fallback=none` در این بازه — Born کامل فعال.
- **2877:** هر دو مدل نزدیک actual — resolve «متعادل».

---

## ۵. ریشه‌یابی ۶ «ایراد»

### ۵.۱ `SKIP forecast — pending unresolved`

| | |
|---|---|
| **علت** | pipeline فقط **یک forecast pending** دارد |
| **محاسبه** | horizon 60s ÷ sample 5s ≈ 8–9 SKIP بین هر RESOLVED |
| **قضاوت** | رفتار **صحیح** — نه باگ |

### ۵.۲ `fallback=saturation_gate` (~65%)

| | |
|---|---|
| **علت** | `born_constructive_v1`: وقتی overshoot > 0.15 یا pred ≥ 0.99 → برگشت به `classical_part` |
| **کد** | `core/quantum_core.py` → `_apply_saturation_gate` |
| **قضاوت** | safeguard frozen — **patch ممنوع** تا production n≥30 |

نسبت به ref `1785119130` (78% gate): born_active **34.6%** vs 22% — کمی بهتر، ولی هنوز اکثریت gate است.

### ۵.۳ خطای per-resolve بالا (`c_err` > 0.5)

| id | act | c | c_err | تفسیر |
|----|-----|---|-------|--------|
| 2860 | 0.118 | 0.288 | 0.554 | flip بازار در 60s |
| 2797 | 0.000 | 0.364 | 0.584 | label=0 (همه sell) |

**علت:** `actual_buy_ratio` در پنجره 60s می‌تواند **0 یا 1** شود — variance ذاتی label. MAE aggregate معنی‌دار است؛ tick-by-tick گمراه‌کننده.

### ۵.۴ شمارنده duplicate (`resolved 273` دوبار)

در log، دو RESOLVED پشت‌سرهم هر دو `resolved 273` چاپ کردند. analyzer رسمی: **280 eligible** — یکی از آن resolveها در running counter log شمارش نشده (احتمالاً همان tick duplicate log). JSON canonical است.

### ۵.۵ قطع شبکه (~13:00)

```text
Network is unreachable → gaierror → NO DATA — retrying
Signal 15 → Stopped at 13:59:51
```

**علت:** DNS/اتصال محلی — نه باگ Huobi logic. run قبل از `max-resolved=500` متوقف شد. production موازی (`1785124007`) همزمان فقط **4 resolve** داشت.

### ۵.۶ `ret` منفی در price sim

```text
81 long  avg_ret=-0.00199  pf=0.0000
```

**diagnostic فقط** — از buy_ratio target جدا است. برای claim استفاده **نمی‌شود**.

---

## ۶. claimهای مجاز / ممنوع

| مجاز | ممنوع |
|------|-------|
| exploratory n=280: q MAE < c MAE (+3.30%) | «مدل سودده شد» |
| infra v5: forward_window 100% | «Born rule ثابت شد» |
| null هنوز بهتر — طبیعی روی 60s | patch Born/gate بدون production gate |
| SKIP و saturation_gate = طراحی | saturation_gate = باگ داده |
| born_active segment +7.18% | significance / paired test |

---

## ۷. checklist

- [x] schema v5، `resolved_label=forward_window` (280/280)
- [x] null baseline گزارش شده (0.2824)
- [x] exploratory 60s جدا از production claim
- [ ] production n≥30 — **هنوز 4/30** (run قطع شد)
- [ ] production n≥720 — **primary claim ممنوع**

---

## ۸. اقدام بعدی

1. رفع اتصال شبکه/DNS محلی.
2. `./scripts/stop_all_runs.sh` سپس restart:
   - `./scripts/start_production_run.sh` (3600s، target 720)
   - `./scripts/start_exploratory_run.sh` (اختیاری، smoke)
3. `./scripts/monitor_live.sh` — تا n≥30 production gate.
4. **هیچ patch مدل** تا gate production برقرار شود.

---

**قبل:** [L08](./08-soal-javab-koli.md)  
**مرجع aggregate:** [L07](./07-tahlil-va-adam-claim.md)
